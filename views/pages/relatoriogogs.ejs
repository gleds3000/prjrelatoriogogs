<%- contentFor('body') %>
  <div class="jumbotron text-center">
     <h1>GOGS - REPOS</h1>
     <div class="row">
     	<% for (repo of repos){%>
     	  <div class="col-sm-4">
     	  	<h2><%= usuario.name %>
     	  	<!--<img class="img-responsive" style="height: 250px" src="<%= usuario.avatar %>">-->
     	  </div>
     	<% } %>
     </div>
  </div>